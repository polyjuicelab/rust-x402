FROM ghcr.io/foundry-rs/foundry:latest

# Foundry image already includes curl, no need to install
# Expose Anvil port
EXPOSE 8545

# Default command
CMD ["anvil", "--host", "0.0.0.0", "--port", "8545"]

